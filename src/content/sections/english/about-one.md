---
enable: true # Control the visibility of this section across all pages where it is used
title: "Your Website Should Be Working Harder Than It Is"

image: "/images/results/headshot_19.jpg"
imagePosition: "left" # Choose between "left" or "right"

description: |
  Most Yorkshire business owners I speak to have the same problem. Decent website, reasonable traffic, but the phone isn't ringing. The website is usually the problem. It shows the business poorly and hides what a customer needs before they get in touch.
  I take time to understand what your customers need to see, then build around that.

button:
  # Refer to the `sharedButton` schema in `src/sections.schema.ts` for all available configuration options (e.g., enable, label, url, hoverEffect, variant, icon, tag, rel, class, target, etc.)
  enable: true
  label: "See How I Work"
  url: "/about/"
  # hoverEffect: "" # Optional: text-flip | creative-fill | magnetic | magnetic-text-flip
  # variant: "" # Optional: fill | outline | text | circle
  # rel: "" # Optional
  # target: "" # Optional
---
