---
interface Props {
  class?: string; // extra classes (e.g. sizing, margin)
  color?: string; // Tailwind text color (defaults to white)
  size?: string;  // Tailwind sizing classes (defaults to w-5 h-5)
}

const {
  class: extraClasses = "",
  color = "text-white",
  size = "w-5 h-5",
} = Astro.props;
---

<svg
  xmlns="http://www.w3.org/2000/svg"
  viewBox="0 0 24 24"
  fill="currentColor"
  class={`${size} ${color} ${extraClasses} shrink-0`}
>
  <path d="M12 17.27L18.18 21l-1.64-7.03L22 9.24l-7.19-.61L12 2 
  9.19 8.63 2 9.24l5.46 4.73L5.82 21z" />
</svg>
